/**
 * Canonical design-system entry point for community-feed consumers.
 *
 * The implementation remains available through the legacy community path while
 * feed and profile surfaces migrate to the explicit community-feed owner.
 */
export {
  CARD_STYLES,
  COLORS,
  EFFECTS,
  INLINE_STYLES,
  RADIUS,
  SPACING,
  TITLE_STYLES,
  TYPOGRAPHY,
  getCardBackground,
  getCardClasses,
  getTitleClasses,
} from "@/core/community/components/styles/communityDesignSystem";
