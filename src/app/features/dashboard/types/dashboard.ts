/**
 * Dashboard types compatibility facade.
 *
 * SSOT types live in shared/types/dashboard.
 */
export type {
  BusinessData,
  AccessPermissions,
  DashboardTab,
  DashboardStats,
  MemberData,
} from "@/shared/types/dashboard";
