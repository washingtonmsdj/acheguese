/**
 * Compat wrapper para manter imports legados funcionando enquanto o hook
 * canônico permanece em `src/core/notifications`.
 */

export { useUnifiedNotifications } from "@/core/notifications/useUnifiedNotifications";
