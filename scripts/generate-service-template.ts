/**
 * Compatibility bridge.
 *
 * Canonical owner: tools/architecture/generate-service-template.ts
 * Keep this path temporarily because package.json exposes `npm run generate:service`.
 */

import "../tools/architecture/generate-service-template";
