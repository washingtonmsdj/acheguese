/**
 * Compatibility bridge.
 *
 * Canonical owner: tools/migrations/generate-migration-template.ts
 * Keep this path temporarily because package.json exposes `npm run generate:migration`.
 */

import "../tools/migrations/generate-migration-template";
