/**
 * Compatibility bridge.
 *
 * Canonical owner: tools/supabase/generate-supabase-types.ts
 * Keep this path temporarily because package.json exposes `npm run generate:types`.
 */

import "../tools/supabase/generate-supabase-types";
