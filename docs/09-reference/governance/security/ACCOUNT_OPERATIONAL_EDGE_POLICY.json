{
  "schemaVersion": "account-operational-edge-policy/v1",
  "guardPattern": "(?:requireOperationalAccount|requireAuthenticatedUser)\\s*\\(",
  "adminRequiredPattern": "require(?:Admin|SuperAdmin)\\s*\\(",
  "blockedStatuses": [
    "scheduled",
    "processing",
    "failed",
    "completed"
  ],
  "guardedKinds": [
    "ai-broker",
    "authenticated-broker",
    "billing-broker",
    "notification-broker",
    "user-data-broker"
  ],
  "systemKinds": [
    "cron-secret",
    "public-auth-broker",
    "public-counter-broker",
    "public-registration-broker",
    "signed-webhook"
  ],
  "allowedDuringDeletion": {
    "privacy-rpc": {
      "reason": "authenticated privacy/status/cancellation channel must remain available during the reversible grace period"
    },
    "user-export-data": {
      "reason": "LGPD subject access/export remains available while the account is pending deletion"
    }
  },
  "blockedLegacy": {
    "user-delete-account": {
      "reason": "legacy destructive handler is separately rollout-blocked and must not become an operational exception"
    }
  },
  "notes": [
    "admin-broker functions are classified admin-only; active admins cannot enter self-service deletion hold",
    "ai-broker functions with an explicit requireAdmin/requireSuperAdmin pattern are classified admin-only instead of guarded",
    "requireAuthenticatedUser is accepted as a guarded path only because the shared businessAuth helper is regression-tested to call requireOperationalAccount after JWT validation",
    "systemKinds do not act as the authenticated end-user through a service-role mutation path",
    "all other serviceRoleAllowlist entries must resolve to one explicit mode or validation fails"
  ]
}
